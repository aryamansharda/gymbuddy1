<!DOCTYPE html>
<html>
  <head>
    <title>Dashboard</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap -->
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/bootstrap-theme.min.css" rel="stylesheet">
    <link href="/css/introHCI.css" rel="stylesheet">
    <link href="css/dashboard.css" rel="stylesheet">
    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
    <![endif]-->
  </head>
  <body id="dashboardBody" class="wrapper">
  <!-- this is a comment in HTML -->
  <div class="titleBackground">
      <h3 align="center" class="pageTitle">Dashboard</h3>
   </div>
  <div class="container">

  <div class="row">
  <div class="pull-right"><button id="helpButton" class="btn btn-primary">Help</button>
  </div>

  <h3>Your Matches Are...</h3>
  <table class="tftable" border="1">
  <tr><th><center>My name is...</center><th><center>I'm free to work out at...</center></th></tr>
    {{#each userProfile}}
        <tr><td>{{name}}</td>
<td>{{#if profile.schedule.0.monday}} Monday @ {{profile.schedule.0.monday}}<br>{{/if}}
    {{#if profile.schedule.0.tuesday}} Tuesday @ {{profile.schedule.0.tuesday}}<br>{{/if}}
    {{#if profile.schedule.0.wednesday}} Wednesday @ {{profile.schedule.0.wednesday}}<br>{{/if}}
    {{#if profile.schedule.0.thursday}} Thursday @ {{profile.schedule.0.thursday}}<br>{{/if}}
    {{#if profile.schedule.0.friday}} Friday @ {{profile.schedule.0.friday}}<br>{{/if}}
    {{#if profile.schedule.0.saturday}} Saturday @ {{profile.schedule.0.saturday}}<br>{{/if}}
    {{#if profile.schedule.0.sunday}} Sunday @ {{profile.schedule.0.sunday}}<br>{{/if}}

        </td></tr>
    {{/each}}

  </table>

    <br>

<div class="row">
<div class="col-md-12"><button id="logoutButton" class="btn btn-primary">Log Out</button>
<span class="pull-right"><button id="myprofilesButton" class="btn btn-primary" pull-right>My Profiles <span aria-hidden="true">&rarr;</span></button></span></div>
</div>
  </div>
    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://code.jquery.com/jquery.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="/js/bootstrap.min.js"></script>
    <script src="/js/dashboard.js"></script>

  </body>
</html>
